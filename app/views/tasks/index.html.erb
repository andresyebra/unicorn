<p style="color: green"><%= notice %></p>

<h1>CREATE A NEW TASK</h1>


  <thead>
   <tr>
      <th>Title</th>
      
      <th>State</th>
      
      <th colspan="2"></th>
    </tr>
  </thead>
 <br>
 <tbody>

    <% @tasks.each do |task| %>
     <tr>
        <td> 
            <%= link_to task.title, task %>
            <%# = task.description %>
        </td>

        <td> <%= task.state  %></td> 
        <td> <%#= task.due_at %></td>
        <td> <%#= task.deleted_at %></td>
        <td> <%#= link_to 'show' , task %></td>
        <td> <%#= link_to 'Edit', edit_task_path{task} %></td>
        <td> <%#= link_to 'Destoy'. task, method: :delete, data: {confirm: 'Are you sure?'} %></td>
     </tr>
     <% end %>
  </tbody>
</table>

<!--
<div id="tasks">
  <% @tasks.each do |task| %>
    <%= render task %>
    <tr>
      <%= link_to "Show this task", task %>
      <td> <%=link_to task.title , task %></td>
    </tr>
  <% end %>
</div>
-->
<br>
<br>
<br>

<%= link_to "New task", new_task_path %>

 